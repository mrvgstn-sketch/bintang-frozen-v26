(function(){
'use strict';
if(window.BFOpenCanonicalSetoran)return;
const KEY='bf_note_setoran_v26',ID='bf-setoran-canonical';
const Core=window.BFCore,Store=window.BFSetoranStore;
if(!Core||!Store)return;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const n=v=>{const x=Number(String(v??'').replace(/\./g,'').replace(',','.'));return Number.isFinite(x)?x:0};
const rp=v=>'Rp '+Math.round(n(v)).toLocaleString('id-ID');
const today=()=>new Date().toISOString().slice(0,10);
const uid=()=>crypto.randomUUID?.()||('set-'+Date.now()+'-'+Math.random().toString(16).slice(2));
const role=()=>String(window.BFCurrentUser?.()?.profile?.role||'').toLowerCase();
const owner=()=>role()==='owner';
const allowed=()=>['owner','admin'].includes(role());
const sb=()=>window.BFSupabase;
const activeCustomers=()=>Core.storage.list('bf_customers').filter(x=>!x.deleted_at&&x.active!==false&&String(x.id||x._bf_uid||'').trim()&&String(x.name||x.nama||'').trim());
const customerId=x=>String(x?.id||x?._bf_uid||'').trim();
const customerName=x=>String(x?.name||x?.nama||'').trim();
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
let editing=null,filter={from:'',to:'',customer:'',method:'ALL',status:'ALL',search:''};
function style(){if(document.getElementById('bf-setoran-guided-style'))return;const s=document.createElement('style');s.id='bf-setoran-guided-style';s.textContent=`#${ID}{position:fixed;inset:0;z-index:2147482000;background:#f4f6fb;overflow:auto}.cs-wrap{max-width:1180px;margin:auto;padding:14px}.cs-head,.cs-card{background:#fff;border:1px solid #e2e8f0;border-radius:14px;padding:14px;margin-bottom:12px}.cs-head{display:flex;gap:10px;align-items:center}.cs-head button{margin-left:auto}.cs-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.cs-field label{display:block;font-size:11px;font-weight:800;color:#475569;margin-bottom:4px}.cs-field input,.cs-field select,.cs-field textarea,.cs-btn{width:100%;box-sizing:border-box;border:1px solid #cbd5e1;border-radius:10px;padding:9px;background:#fff;font-size:13px}.cs-btn{width:auto;font-weight:800;cursor:pointer}.cs-primary{background:#0d1b3e;color:#fff;border-color:#0d1b3e}.cs-good{background:#047857;color:#fff;border-color:#047857}.cs-warn{background:#b45309;color:#fff;border-color:#b45309}.cs-danger{background:#b91c1c;color:#fff;border-color:#b91c1c}.cs-full{grid-column:1/-1}.cs-notes,.cs-payments{display:grid;gap:7px}.cs-note-row{display:flex;gap:7px}.cs-payment-row{display:grid;grid-template-columns:1fr 1.2fr 1fr 1.4fr auto;gap:7px;align-items:end;border:1px solid #e2e8f0;border-radius:10px;padding:8px;background:#f8fafc}.cs-note-row input{flex:1}.cs-calc{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:10px}.cs-kpi{border-radius:11px;padding:10px;background:#f8fafc;border:1px solid #e2e8f0}.cs-kpi small{display:block;color:#64748b}.cs-kpi b{font-size:16px}.cs-alert{padding:9px 11px;border-radius:10px;background:#fff7ed;border:1px solid #fed7aa;color:#9a3412;font-size:12px;margin-top:8px}.cs-filter{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.cs-summary{display:flex;gap:7px;flex-wrap:wrap;margin:10px 0}.cs-pill{padding:5px 8px;border-radius:999px;background:#eef2ff;font-size:11px}.cs-table{width:100%;border-collapse:collapse}.cs-table th,.cs-table td{padding:8px;border-bottom:1px solid #e2e8f0;text-align:left;font-size:11px;vertical-align:top}.cs-group td{background:#eef2ff;color:#0d1b3e}.cs-mobile{display:none}.cs-status{font-weight:800}.cs-pending{color:#b45309}.cs-done{color:#047857}.cs-correction{color:#b91c1c}@media(max-width:800px){.cs-grid,.cs-filter,.cs-calc{grid-template-columns:1fr}.cs-payment-row{grid-template-columns:1fr}.cs-full{grid-column:auto}.cs-table-wrap{display:none}.cs-mobile{display:grid;gap:8px}.cs-mobile-card{background:#fff;border:1px solid #e2e8f0;border-radius:12px;padding:11px}}`;document.head.appendChild(s)}
function businessStatus(r){if(r.cancelled_at||r.deleted_at)return'Dibatalkan';if(r.commission_status==='PENDING_OWNER')return'Menunggu Konfirmasi Owner';if(r.commission_status==='CORRECTION_REQUIRED')return'Perlu Koreksi Admin';if(r.commission_status==='APPROVED')return'Komisi Disetujui';if(r.commission_status==='REJECTED')return'Komisi Ditolak';if(r.commission_status==='PAID')return'Komisi Dibayar';if(r.commission_sync_status==='PENDING'||r.commission_sync_status==='ERROR')return'Pengajuan Komisi Belum Tersinkron';if(r.business_status)return r.business_status;if(r.customer_funds_case_id&&r.flow_mode!=='SETORAN_GUIDED')return'Catatan Lama / Dana Customer';return'Selesai — Tidak Ada Komisi'}
function legacyMethod(r){const code=String(r?.payment_method_code||'').toUpperCase(),dest=String(r?.destination_account||'').trim();if(code==='TUNAI')return{method:'TUNAI',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:''};if(code==='QRIS')return{method:'QRIS',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:dest};if(code==='TRANSFER')return{method:'TRANSFER',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:dest};const raw=String(r?.metode||r?.via_bank||r?.via||'').trim(),k=raw.toUpperCase();if(k==='CASH'||k==='TUNAI')return{method:'TUNAI',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:''};if(['BCA','BNI','BRI','MANDIRI','KALBAR','BSI'].includes(k))return{method:'TRANSFER',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:k};if(k==='QRIS')return{method:'QRIS',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:''};return{method:'TUNAI',amount:n(r.gross_transfer??r.nominal??r.jumlah),destination_account:''}}
function paymentComponents(r){const a=Array.isArray(r?.payment_components)?r.payment_components.filter(x=>x&&n(x.amount)>0):[];return a.length?a.map(x=>({method:String(x.method||'TUNAI').toUpperCase(),amount:n(x.amount),destination_account:String(x.destination_account||''),proof_url:String(x.proof_url||'')})):[legacyMethod(r)]}
function paymentSummary(r){const a=paymentComponents(r),labels=a.map(x=>x.method==='TUNAI'?'Tunai':x.method==='QRIS'?'QRIS':`Transfer${x.destination_account?' '+x.destination_account:''}`);return{key:a.length>1?'MIXED':a[0]?.method||'MISSING',label:[...new Set(labels)].join(' + ')||'Metode Tidak Tercatat',components:a}}
function noteAmounts(r){const a=Array.isArray(r?.settled_note_amounts)?r.settled_note_amounts.map(n).filter(x=>x>0):[];return a.length?a:[]}
function noteTotal(r){const a=noteAmounts(r);return a.length?a.reduce((s,x)=>s+x,0):n(r?.settled_note_total)}
function calc(gross,notes,payments){const noteTotal=(notes||[]).map(n).filter(x=>x>0).reduce((s,x)=>s+x,0),paymentTotal=(payments||[]).reduce((s,x)=>s+n(x.amount),0);return{noteTotal,paymentTotal,difference:n(gross)-noteTotal,paymentDifference:n(gross)-paymentTotal}}
function filtered(rows){const q=filter.search.trim().toLowerCase(),cq=filter.customer.trim().toLowerCase();return rows.filter(r=>{const d=String(r.tanggal||'').slice(0,10),m=paymentSummary(r),st=businessStatus(r);if(filter.from&&d<filter.from)return false;if(filter.to&&d>filter.to)return false;if(cq&&!String(r.customer_name_snapshot||r.customer||r.namaCustomer||'').toLowerCase().includes(cq))return false;if(filter.method!=='ALL'&&m.key!==filter.method)return false;if(filter.status!=='ALL'&&st!==filter.status)return false;if(q&&!([r.id,r.customer_name_snapshot,r.customer,r.namaCustomer,r.keterangan,m.label,st].join(' ').toLowerCase().includes(q)))return false;return true})}
async function cloudAck(){if(!window.BFCloud?.enabled?.())return true;let ok=await window.BFCloud.push();if(!ok)return false;for(let i=0;i<12;i++){if(!window.BFCloud.isDirty?.(KEY))return true;await sleep(150)}return !window.BFCloud.isDirty?.(KEY)}
async function rpc(name,args){if(!sb())throw new Error('Database belum siap. Muat ulang aplikasi.');const {data,error}=await sb().rpc(name,args);if(error)throw error;return Array.isArray(data)&&data.length===1?data[0]:data}
async function registerCase(row,proofs){const summary=paymentSummary(row);return rpc('bf_cf_record_case',{p_source_setoran_id:String(row.id),p_customer_id:String(row.customer_id),p_customer_name:String(row.customer_name_snapshot),p_gross:n(row.gross_transfer),p_transfer_date:row.tanggal,p_actual_sender:null,p_method:summary.key,p_proofs:proofs,p_note:row.keterangan||null,p_idempotency:'SETORAN:'+String(row.id)})}
async function submitFlow(row,caseId){return rpc('bf_cf_submit_setoran',{p_case:caseId,p_customer_id:String(row.customer_id),p_customer_name:String(row.customer_name_snapshot),p_gross:n(row.gross_transfer),p_transfer_date:row.tanggal,p_payment_components:row.payment_components,p_note_amounts:row.settled_note_amounts,p_request_commission:!!row.commission_requested,p_agreement:row.commission_agreement||null,p_idempotency:'SETORAN-COMMISSION:'+String(row.id)})}
function modalCustomer(select){document.getElementById('bf-active-customer-picker')?.remove();const all=activeCustomers(),o=document.createElement('div');o.id='bf-active-customer-picker';o.style.cssText='position:fixed;inset:0;z-index:2147483647;background:#0f172acc;display:flex;align-items:center;justify-content:center;padding:12px';o.innerHTML=`<div style="width:min(620px,96vw);max-height:86vh;background:#fff;border-radius:15px;overflow:hidden"><div style="padding:12px 14px;background:#0d1b3e;color:#fff;display:flex;justify-content:space-between"><div><b>Pilih Customer Aktif</b><small style="display:block">Hanya dari Data Master Customer aktif</small></div><button data-close>✕</button></div><div style="padding:12px"><input data-q type="search" placeholder="Cari nama / kontak..." style="width:100%;box-sizing:border-box;padding:10px"><div data-list style="max-height:55vh;overflow:auto;margin-top:8px"></div></div></div>`;document.body.appendChild(o);const q=o.querySelector('[data-q]'),list=o.querySelector('[data-list]);
}
function customerPicker(select){document.getElementById('bf-active-customer-picker')?.remove();const all=activeCustomers(),o=document.createElement('div');o.id='bf-active-customer-picker';o.style.cssText='position:fixed;inset:0;z-index:2147483647;background:#0f172acc;display:flex;align-items:center;justify-content:center;padding:12px';o.innerHTML=`<div style="width:min(620px,96vw);max-height:86vh;background:#fff;border-radius:15px;overflow:hidden"><div style="padding:12px 14px;background:#0d1b3e;color:#fff;display:flex;justify-content:space-between"><div><b>Pilih Customer Aktif</b><small style="display:block">Hanya dari Data Master Customer aktif</small></div><button data-close>✕</button></div><div style="padding:12px"><input data-q type="search" placeholder="Cari nama / kontak..." style="width:100%;box-sizing:border-box;padding:10px"><div data-list style="max-height:55vh;overflow:auto;margin-top:8px"></div></div></div>`;document.body.appendChild(o);const q=o.querySelector('[data-q]'),list=o.querySelector('[data-list]');const draw=()=>{const s=q.value.toLowerCase();list.innerHTML=all.filter(c=>!s||[customerName(c),c.wa,c.phone,c.contact,c.alamat,c.address].some(v=>String(v||'').toLowerCase().includes(s))).map(c=>`<button data-id="${esc(customerId(c))}" style="display:block;width:100%;text-align:left;padding:10px;margin:4px 0;border:1px solid #e2e8f0;border-radius:9px;background:#fff"><b>${esc(customerName(c))}</b><small style="display:block;color:#64748b">${esc(c.wa||c.phone||c.contact||'Tanpa kontak')}</small></button>`).join('')||'<div style="padding:16px;text-align:center">Tidak ada Customer aktif.</div>'};o.onclick=e=>{if(e.target===o||e.target.closest('[data-close]'))return o.remove();const b=e.target.closest('[data-id]');if(!b)return;select.value=b.dataset.id;select.dispatchEvent(new Event('change',{bubbles:true}));o.remove()};q.oninput=draw;draw();q.focus()}
function shell(){document.getElementById(ID)?.remove();style();const el=document.createElement('div');el.id=ID;el.innerHTML=`<div class="cs-wrap"><div class="cs-head"><div><b>Catatan Setoran</b><small style="display:block;color:#64748b">Satu Setoran dapat memakai beberapa metode pembayaran</small></div><button class="cs-btn" data-close>✕ Tutup</button></div><div id="cs-body"></div></div>`;document.body.appendChild(el);el.querySelector('[data-close]').onclick=()=>el.remove();return el}
function render(){const el=document.getElementById(ID)||shell(),body=el.querySelector('#cs-body');const rows=Store.list().filter(x=>!x.deleted_at).sort((a,b)=>String(b.tanggal||'').localeCompare(String(a.tanggal||''))||String(b.updated_at||'').localeCompare(String(a.updated_at||'')));const r=editing?rows.find(x=>x.id===editing)||{}:{};const oldNotes=noteAmounts(r),notes=oldNotes.length?oldNotes:[0],oldPayments=r.id?paymentComponents(r):[{method:'TUNAI',amount:0,destination_account:'',proof_url:''}];const lock=!!r.id&&(r.commission_status==='APPROVED'||r.commission_status==='PAID'||(r.customer_funds_case_id&&r.flow_mode!=='SETORAN_GUIDED'));const customers=activeCustomers(),statuses=[...new Set(rows.map(businessStatus))].sort();body.innerHTML=`<div class="cs-card"><h3 style="margin-top:0">${r.id?'Edit Setoran':'Tambah Setoran'}</h3>${lock?'<div class="cs-alert">Catatan ini sudah memiliki proses finansial lanjutan dan tidak dapat diedit langsung. Owner masih dapat melakukan pembatalan terkontrol bila downstream aman.</div>':''}<div class="cs-grid"><div class="cs-field"><label>Tanggal</label><input id="cs-date" type="date" value="${esc(r.tanggal||today())}" ${lock?'disabled':''}></div><div class="cs-field"><label>Customer</label><select id="cs-customer" ${lock?'disabled':''}><option value="">Pilih Customer...</option>${customers.map(c=>`<option value="${esc(customerId(c))}" ${String(r.customer_id||'')===customerId(c)?'selected':''}>${esc(customerName(c))}</option>`).join('')}</select></div><div class="cs-field"><label>Nominal Setoran</label><input id="cs-gross" inputmode="numeric" value="${esc(r.gross_transfer??r.nominal??r.jumlah??'')}" ${lock?'disabled':''}></div><div class="cs-field cs-full"><label>Rincian Metode Pembayaran</label><div id="cs-payments" class="cs-payments"></div><button type="button" class="cs-btn" id="cs-add-payment" ${lock?'disabled':''}>＋ Tambah Metode Pembayaran</button><div class="cs-alert">Total seluruh metode pembayaran wajib sama dengan Nominal Setoran.</div></div><div class="cs-field cs-full"><label>Nota yang Sudah Lunas — nilai saja, boleh lebih dari satu</label><div class="cs-notes" id="cs-notes"></div><button type="button" class="cs-btn" id="cs-add-note" ${lock?'disabled':''}>＋ Tambah Nota Lunas</button></div><div class="cs-field cs-full"><label>Keterangan</label><textarea id="cs-note" ${lock?'disabled':''}>${esc(r.keterangan||'')}</textarea></div><div class="cs-field cs-full"><div class="cs-calc"><div class="cs-kpi"><small>Total Pembayaran</small><b id="cs-payment-total">Rp 0</b></div><div class="cs-kpi"><small>Total Nota Lunas</small><b id="cs-note-total">Rp 0</b></div><div class="cs-kpi"><small>Selisih Setoran</small><b id="cs-diff">Rp 0</b></div><div class="cs-kpi"><small>Komisi Diajukan</small><b id="cs-commission">Rp 0</b></div></div></div><div class="cs-field cs-full"><label><input id="cs-request-commission" type="checkbox" style="width:auto" ${r.commission_requested?'checked':''} ${lock?'disabled':''}> Ajukan kelebihan sebagai Komisi Customer</label><small style="color:#64748b">Nominal Komisi tidak dapat diedit; dihitung otomatis dari selisih.</small></div><div class="cs-field cs-full" id="cs-agreement-wrap"><label>Catatan / Kesepakatan Komisi</label><textarea id="cs-agreement" ${lock?'disabled':''}>${esc(r.commission_agreement||'')}</textarea></div></div><div style="display:flex;gap:8px;margin-top:12px"><button class="cs-btn cs-primary" id="cs-save" ${lock?'disabled':''}>${r.id?'Simpan Koreksi':'Simpan Setoran'}</button>${r.id?'<button class="cs-btn" id="cs-cancel-edit">Batal Edit</button>':''}</div></div><div class="cs-card"><h3 style="margin-top:0">Histori Setoran</h3><div class="cs-filter"><div class="cs-field"><label>Dari Tanggal</label><input data-f="from" type="date" value="${esc(filter.from)}"></div><div class="cs-field"><label>Sampai Tanggal</label><input data-f="to" type="date" value="${esc(filter.to)}"></div><div class="cs-field"><label>Customer</label><input data-f="customer" value="${esc(filter.customer)}" placeholder="Cari Customer"></div><div class="cs-field"><label>Metode</label><select data-f="method"><option value="ALL">Semua Metode</option><option value="TUNAI">Tunai</option><option value="TRANSFER">Transfer</option><option value="QRIS">QRIS</option><option value="MIXED">Kombinasi</option></select></div><div class="cs-field"><label>Status</label><select data-f="status"><option value="ALL">Semua Status</option>${statuses.map(s=>`<option ${filter.status===s?'selected':''}>${esc(s)}</option>`).join('')}</select></div><div class="cs-field"><label>Pencarian</label><input data-f="search" value="${esc(filter.search)}" placeholder="Customer / keterangan"></div></div><button class="cs-btn" id="cs-reset" style="margin-top:8px">Atur Ulang Filter</button><div id="cs-history"></div></div>`;
 let currentNotes=[...notes],currentPayments=oldPayments.map(x=>({...x})),notesEl=body.querySelector('#cs-notes'),paymentsEl=body.querySelector('#cs-payments');
 function drawNotes(){notesEl.innerHTML=currentNotes.map((v,i)=>`<div class="cs-note-row"><input inputmode="numeric" data-note="${i}" value="${v?esc(v):''}" placeholder="Nilai Nota ${i+1}" ${lock?'disabled':''}><button type="button" class="cs-btn" data-del-note="${i}" ${currentNotes.length<=1||lock?'disabled':''}>✕</button></div>`).join('');notesEl.querySelectorAll('[data-note]').forEach(x=>x.oninput=()=>{currentNotes[Number(x.dataset.note)]=n(x.value);updateCalc()});notesEl.querySelectorAll('[data-del-note]').forEach(x=>x.onclick=()=>{currentNotes.splice(Number(x.dataset.delNote),1);drawNotes();updateCalc()})}
 function drawPayments(){paymentsEl.innerHTML=currentPayments.map((p,i)=>`<div class="cs-payment-row" data-payment-row="${i}"><div class="cs-field"><label>Metode</label><select data-pay-method="${i}" ${lock?'disabled':''}><option value="TUNAI" ${p.method==='TUNAI'?'selected':''}>Tunai</option><option value="TRANSFER" ${p.method==='TRANSFER'?'selected':''}>Transfer</option><option value="QRIS" ${p.method==='QRIS'?'selected':''}>QRIS</option></select></div><div class="cs-field"><label>Rekening / Tujuan</label><input data-pay-dest="${i}" value="${esc(p.destination_account||'')}" placeholder="Wajib untuk Transfer" ${p.method!=='TRANSFER'?'disabled':''} ${lock?'disabled':''}></div><div class="cs-field"><label>Nominal</label><input data-pay-amount="${i}" inputmode="numeric" value="${p.amount?esc(p.amount):''}" ${lock?'disabled':''}></div><div class="cs-field"><label>Bukti (opsional)</label><input data-pay-proof="${i}" type="file" accept="image/*" ${lock?'disabled':''}>${p.proof_url?`<small style="display:block;color:#047857">Bukti tersimpan</small>`:''}</div><button type="button" class="cs-btn cs-danger" data-del-payment="${i}" ${currentPayments.length<=1||lock?'disabled':''}>✕</button></div>`).join('');paymentsEl.querySelectorAll('[data-pay-method]').forEach(x=>x.onchange=()=>{const i=Number(x.dataset.payMethod);currentPayments[i].method=x.value;if(x.value!=='TRANSFER')currentPayments[i].destination_account='';drawPayments();updateCalc()});paymentsEl.querySelectorAll('[data-pay-dest]').forEach(x=>x.oninput=()=>currentPayments[Number(x.dataset.payDest)].destination_account=x.value);paymentsEl.querySelectorAll('[data-pay-amount]').forEach(x=>x.oninput=()=>{currentPayments[Number(x.dataset.payAmount)].amount=n(x.value);updateCalc()});paymentsEl.querySelectorAll('[data-del-payment]').forEach(x=>x.onclick=()=>{currentPayments.splice(Number(x.dataset.delPayment),1);drawPayments();updateCalc()})}
 function updateCalc(){const c=calc(body.querySelector('#cs-gross').value,currentNotes,currentPayments),req=body.querySelector('#cs-request-commission');body.querySelector('#cs-payment-total').textContent=rp(c.paymentTotal);body.querySelector('#cs-note-total').textContent=rp(c.noteTotal);body.querySelector('#cs-diff').textContent=rp(c.difference);if(c.difference<=0&&req.checked)req.checked=false;req.disabled=lock||c.difference<=0;body.querySelector('#cs-commission').textContent=rp(req.checked?c.difference:0);body.querySelector('#cs-agreement-wrap').style.display=req.checked?'block':'none'}
 drawNotes();drawPayments();updateCalc();body.querySelector('#cs-add-note').onclick=()=>{currentNotes.push(0);drawNotes();updateCalc()};body.querySelector('#cs-add-payment').onclick=()=>{currentPayments.push({method:'TUNAI',amount:0,destination_account:'',proof_url:''});drawPayments();updateCalc()};body.querySelector('#cs-gross').oninput=updateCalc;body.querySelector('#cs-request-commission').onchange=updateCalc;const cust=body.querySelector('#cs-customer');if(!lock){cust.style.display='none';const b=document.createElement('button');b.type='button';b.className='cs-btn';b.id='cs-customer-pic';const selected=customers.find(c=>customerId(c)===cust.value);b.textContent=selected?customerName(selected):'Pilih Customer Aktif';cust.after(b);b.onclick=()=>customerPicker(cust);cust.onchange=()=>{const c=customers.find(x=>customerId(x)===cust.value);b.textContent=c?customerName(c):'Pilih Customer Aktif'}}body.querySelector('#cs-cancel-edit')?.addEventListener('click',()=>{editing=null;render()});body.querySelector('#cs-save').onclick=()=>save(currentNotes,currentPayments);body.querySelectorAll('[data-f]').forEach(x=>{x.value=filter[x.dataset.f];x.addEventListener(x.tagName==='SELECT'||x.type==='date'?'change':'input',()=>{filter[x.dataset.f]=x.value;renderHistory(rows)})});body.querySelector('#cs-reset').onclick=()=>{filter={from:'',to:'',customer:'',method:'ALL',status:'ALL',search:''};render()};renderHistory(rows)}
function actions(r){let out='';const editable=(!r.commission_status||['CORRECTION_REQUIRED','REJECTED'].includes(r.commission_status))&&(!r.customer_funds_case_id||r.flow_mode==='SETORAN_GUIDED');if(editable)out+=`<button class="cs-btn" data-edit="${esc(r.id)}">Edit</button> `;if(r.commission_sync_status==='PENDING'||r.commission_sync_status==='ERROR')out+=`<button class="cs-btn cs-warn" data-retry="${esc(r.id)}">Coba Lagi Komisi</button> `;if(owner()&&r.commission_status==='PENDING_OWNER'&&r.commission_classification_id)out+=`<button class="cs-btn cs-good" data-approve-commission="${esc(r.id)}">Setujui Komisi</button> <button class="cs-btn cs-warn" data-correct-commission="${esc(r.id)}">Minta Koreksi</button> <button class="cs-btn" data-reject-commission="${esc(r.id)}">Tolak</button> `;if(owner())out+=`<button class="cs-btn cs-danger" data-cancel-setoran="${esc(r.id)}">Batalkan Setoran</button>`;return out||'—'}
function renderHistory(rows){const el=document.getElementById('cs-history');if(!el)return;const list=filtered(rows),total=list.reduce((s,r)=>s+n(r.gross_transfer??r.nominal??r.jumlah),0);const summary=`<div class="cs-summary"><span class="cs-pill"><b>${list.length}</b> Setoran</span><span class="cs-pill"><b>${rp(total)}</b> Total</span></div>`;const row=r=>{const m=paymentSummary(r),st=businessStatus(r),nt=noteTotal(r),comm=n(r.commission_final??r.commission_amount_calculated??0);return`<tr><td>${esc(r.tanggal||'-')}</td><td><b>${esc(r.customer_name_snapshot||r.customer||r.namaCustomer||'-')}</b></td><td>${rp(r.gross_transfer??r.nominal??r.jumlah)}</td><td>${esc(m.label)}${m.components.length>1?`<br><small>${m.components.map(x=>`${x.method} ${rp(x.amount)}`).join(' • ')}</small>`:''}</td><td>${nt?rp(nt):'Catatan Lama'}</td><td class="cs-status ${st.includes('Menunggu')?'cs-pending':st.includes('Koreksi')?'cs-correction':'cs-done'}">${esc(st)}${comm?'<br><small>Komisi '+rp(comm)+'</small>':''}</td><td>${actions(r)}</td></tr>`};const card=r=>{const m=paymentSummary(r),st=businessStatus(r);return`<div class="cs-mobile-card"><div style="display:flex;justify-content:space-between"><b>${esc(r.customer_name_snapshot||r.customer||r.namaCustomer||'-')}</b><b>${rp(r.gross_transfer??r.nominal??r.jumlah)}</b></div><small>${esc(r.tanggal||'-')} • ${esc(m.label)}</small><div class="cs-status">${esc(st)}</div><div style="margin-top:8px">${actions(r)}</div></div>`};el.innerHTML=summary+`<div class="cs-table-wrap"><table class="cs-table"><thead><tr><th>Tanggal</th><th>Customer</th><th>Nominal Setoran</th><th>Metode</th><th>Total Nota Lunas</th><th>Status</th><th>Aksi</th></tr></thead><tbody>${list.map(row).join('')||'<tr><td colspan="7">Tidak ada Setoran sesuai filter.</td></tr>'}</tbody></table></div><div class="cs-mobile">${list.map(card).join('')||'<div class="cs-mobile-card">Tidak ada Setoran sesuai filter.</div>'}</div>`;el.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{editing=b.dataset.edit;render()});el.querySelectorAll('[data-retry]').forEach(b=>b.onclick=()=>retryFlow(b.dataset.retry));el.querySelectorAll('[data-approve-commission]').forEach(b=>b.onclick=()=>decideCommission(b.dataset.approveCommission,'APPROVE'));el.querySelectorAll('[data-correct-commission]').forEach(b=>b.onclick=()=>decideCommission(b.dataset.correctCommission,'CORRECTION_REQUIRED'));el.querySelectorAll('[data-reject-commission]').forEach(b=>b.onclick=()=>decideCommission(b.dataset.rejectCommission,'REJECT'));el.querySelectorAll('[data-cancel-setoran]').forEach(b=>b.onclick=()=>cancelSetoran(b.dataset.cancelSetoran))}
async function uploadComponentProofs(payments,rowId){const out=[];for(let i=0;i<payments.length;i++){const p={...payments[i]},input=document.querySelector(`[data-pay-proof="${i}"]`),file=input?.files?.[0];if(file&&window.BFPhotoStorage){const up=await window.BFPhotoStorage.uploadFile(file,{transactionNo:String(rowId),supplier:`bukti-setoran-${i+1}`});p.proof_url=up?.url||p.proof_url||''}out.push({method:String(p.method).toUpperCase(),amount:n(p.amount),destination_account:p.method==='TRANSFER'?String(p.destination_account||'').trim():'',proof_url:p.proof_url||''})}return out}
async function save(notes,payments){const body=document.getElementById('cs-body'),id=editing||uid(),old=editing?Store.list().find(x=>x.id===editing):null,customer=activeCustomers().find(c=>customerId(c)===body.querySelector('#cs-customer').value);if(!customer)return alert('Customer wajib dipilih dari Data Master Customer yang masih aktif.');const gross=n(body.querySelector('#cs-gross').value),validNotes=notes.map(n).filter(x=>x>0),request=body.querySelector('#cs-request-commission').checked,agreement=body.querySelector('#cs-agreement').value.trim();if(gross<=0)return alert('Nominal Setoran harus lebih dari 0.');if(!validNotes.length)return alert('Masukkan minimal satu nilai Nota yang sudah lunas.');const normalized=payments.map(x=>({method:String(x.method||'').toUpperCase(),amount:n(x.amount),destination_account:String(x.destination_account||'').trim(),proof_url:String(x.proof_url||'')}));if(!normalized.length||normalized.some(x=>!['TUNAI','TRANSFER','QRIS'].includes(x.method)||x.amount<=0))return alert('Setiap metode pembayaran wajib memiliki metode dan nominal lebih dari 0.');if(normalized.some(x=>x.method==='TRANSFER'&&!x.destination_account))return alert('Rekening tujuan wajib diisi untuk setiap pembayaran Transfer.');const paymentTotal=normalized.reduce((s,x)=>s+x.amount,0);if(Math.abs(paymentTotal-gross)>0.0001)return alert(`Total rincian pembayaran ${rp(paymentTotal)} harus sama dengan Nominal Setoran ${rp(gross)}.`);const c=calc(gross,validNotes,normalized);if(request&&c.difference<=0)return alert('Komisi hanya dapat diajukan jika ada kelebihan Setoran.');if(request&&!agreement)return alert('Catatan/Kesepakatan wajib untuk pengajuan Komisi.');const before=Store.list();try{const uploaded=await uploadComponentProofs(normalized,id),summaryCode=uploaded.length>1?'MIXED':uploaded[0].method,summaryLabel=uploaded.length>1?'MIXED':summaryCode,row={...(old||{}),id,tanggal:body.querySelector('#cs-date').value,customer_id:customerId(customer),customer_name_snapshot:customerName(customer),gross_transfer:gross,actual_sender:null,payment_components:uploaded,payment_method_code:summaryCode,destination_account:uploaded.length===1&&uploaded[0].method==='TRANSFER'?uploaded[0].destination_account:'',metode:summaryLabel,settled_note_amounts:validNotes,settled_note_total:c.noteTotal,difference_amount:c.difference,commission_requested:request,commission_amount_calculated:request?c.difference:0,commission_agreement:request?agreement:null,commission_status:request?'PENDING_OWNER':null,business_status:request?'Menunggu Konfirmasi Owner':'Selesai — Tidak Ada Komisi',flow_mode:'SETORAN_GUIDED',keterangan:body.querySelector('#cs-note').value.trim(),bukti_foto:uploaded.find(x=>x.proof_url)?.proof_url||old?.bukti_foto||null,updated_at:new Date().toISOString()};Store.write(before.filter(x=>x.id!==id).concat(row));if(!(await cloudAck()))throw new Error('GAGAL / BELUM TERSINKRON — Setoran belum mendapat konfirmasi sinkronisasi cloud.');let caseData=row.customer_funds_case_id?{id:row.customer_funds_case_id,case_no:row.customer_funds_case_no}:await registerCase(row,uploaded.map(x=>x.proof_url).filter(Boolean));const caseObj=Array.isArray(caseData)?caseData[0]:caseData;row.customer_funds_case_id=caseObj?.id||caseObj?.case_id||row.customer_funds_case_id;row.customer_funds_case_no=caseObj?.case_no||row.customer_funds_case_no;try{const f=await submitFlow(row,row.customer_funds_case_id);row.customer_funds_case_revision=f?.case_revision||row.customer_funds_case_revision;row.commission_classification_id=f?.classification_id||null;row.commission_status=f?.classification_status||null;row.business_status=request?'Menunggu Konfirmasi Owner':'Selesai — Tidak Ada Komisi';row.commission_sync_status='SYNCED';row.commission_sync_error=null}catch(e){row.commission_sync_status='ERROR';row.commission_sync_error=e.message||String(e)}Store.write(Store.list().filter(x=>x.id!==id).concat(row));await cloudAck();editing=null;render();if(row.commission_sync_status==='ERROR')alert('Setoran tersimpan, tetapi proses Dana Customer belum tersinkron. Gunakan Coba Lagi Komisi.');else alert(request?'Setoran tersimpan dan Komisi diajukan ke Owner.':'Setoran tersimpan.')}catch(e){Store.write(before);await window.BFCloud?.push?.();alert(e.message||String(e))}}
async function retryFlow(id){const row=Store.list().find(x=>x.id===id);if(!row)return;try{let caseId=row.customer_funds_case_id;if(!caseId){const c=await registerCase(row,paymentComponents(row).map(x=>x.proof_url).filter(Boolean));caseId=(Array.isArray(c)?c[0]:c)?.id}const f=await submitFlow(row,caseId),next={...row,customer_funds_case_id:caseId,customer_funds_case_revision:f?.case_revision||row.customer_funds_case_revision,commission_classification_id:f?.classification_id||row.commission_classification_id,commission_status:f?.classification_status||row.commission_status,commission_sync_status:'SYNCED',commission_sync_error:null};Store.write(Store.list().filter(x=>x.id!==id).concat(next));await cloudAck();render()}catch(e){alert(e.message||String(e))}}
async function decideCommission(id,action){const row=Store.list().find(x=>x.id===id);if(!row||!owner()||!row.commission_classification_id)return;let fee=0,bearer='NONE',note='';if(action==='APPROVE'){fee=n(prompt('Potongan biaya bank (isi 0 jika tidak ada):','0')||0);if(fee<0)return alert('Potongan bank tidak boleh negatif.');if(fee>0)bearer=confirm('Apakah potongan dibebankan ke Customer?\nOK = Customer\nBatal = Bintang Frozen')?'CUSTOMER':'BF';note=prompt('Catatan persetujuan Owner (opsional):')||''}else if(action==='CORRECTION_REQUIRED'){note=prompt('Alasan koreksi yang harus dilakukan Admin:')||'';if(!note.trim())return}else{if(!confirm('Tolak pengajuan Komisi Customer ini?'))return;note=prompt('Catatan penolakan (opsional):')||''}try{const res=await rpc('bf_cf_decide_setoran_commission',{p_classification:row.commission_classification_id,p_action:action,p_bank_fee:fee,p_fee_bearer:bearer,p_note:note||null}),next={...row,commission_status:res?.status||row.commission_status,commission_final:res?.commission_final,updated_at:new Date().toISOString()};if(action==='APPROVE')next.business_status='Komisi Disetujui';if(action==='CORRECTION_REQUIRED')next.business_status='Perlu Koreksi Admin';if(action==='REJECT')next.business_status='Komisi Ditolak';Store.write(Store.list().filter(x=>x.id!==id).concat(next));await cloudAck();render()}catch(e){alert(e.message||String(e))}}
async function cancelSetoran(id){const row=Store.list().find(x=>x.id===id);if(!row||!owner())return;const reason=prompt('Alasan pembatalan Setoran (wajib):')||'';if(!reason.trim())return;if(!confirm(`Batalkan Setoran ${row.customer_name_snapshot||row.customer||''} sebesar ${rp(row.gross_transfer??row.nominal??row.jumlah)}?\n\nTransaksi tidak akan dihapus permanen. Jejak audit tetap dipertahankan.`))return;const before=Store.list();try{let result={status:'NO_CASE'};if(row.customer_funds_case_id)result=await rpc('bf_cf_cancel_setoran',{p_source_setoran_id:String(row.id),p_reason:reason.trim(),p_expected_revision:row.customer_funds_case_revision??null});const now=new Date().toISOString(),next={...row,cancelled_at:now,cancelled_by:window.BFCurrentUser?.()?.profile?.display_name||'Owner',cancel_reason:reason.trim(),deleted_at:now,deleted_by:window.BFCurrentUser?.()?.profile?.display_name||'Owner',business_status:'Dibatalkan',customer_funds_case_revision:result?.case_revision||row.customer_funds_case_revision,updated_at:now};Store.write(before.filter(x=>x.id!==id).concat(next));if(!(await cloudAck()))throw new Error('Pembatalan belum tersinkron ke cloud.');render();alert('Setoran dibatalkan. Jejak transaksi tetap tersimpan untuk audit.')}catch(e){Store.write(before);await window.BFCloud?.push?.();alert(e.message||String(e))}}
function open(){if(!allowed())return alert('Akses Catatan Setoran hanya untuk Owner/Admin.');shell();render()}
window.BFOpenCanonicalSetoran=open;
})();