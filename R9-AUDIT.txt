BINTANG FROZEN V26 — R9 OPERATIONS & OWNER AUDIT

Baseline: Adaptive UI R8 supplied by user.

Changes:
1. Mobile menu geometry corrected at the actual drawer/container and child-grid level.
2. Barang Keluar marketing is canonical per customer group; legacy global marketing is read as fallback.
3. Customer field is searchable using existing master customer names, with WA/contact shown in datalist labels.
4. Active finance CRUD attachments support arrays of Supabase Storage URLs; legacy single-photo fields remain readable.
5. Barang Masuk supplier attachments retain existing multi-photo Storage flow.
6. Owner report rebuilt around one dataset aggregator with explicit date range, detailed operational/finance tables,
   employee work summary, evidence-based review flags, and separate Supabase audit-log activity.
7. Current-state business transactions are deduplicated before totals; audit events are not counted as transactions.
8. No Auth/RLS/database/store-code/realtime/cloud-startup change.

Data compatibility:
- Barang Keluar legacy `marketing` is inherited by each customer only when customer-level marketing is absent.
- New outgoing records store `customers[].marketing`; top-level `marketing` remains a compatibility summary.
- Finance stores `bukti_fotos[]` plus `bukti_foto` first-item compatibility field.
- Incoming supplier photos continue to store `nota_fotos[]` plus `nota_foto`.

No SQL migration is required.
